.env:
  directory: .
  filename: .env
  prefix: ""

spring:
  config:
    import: optional:file:.env[.properties]
  data:
    mongodb:
      uri: ${URI}
      database: FilmesApollo70
  rabbitmq:
    host: localhost
    port: 5672
    username: rabbitmq
    password: rabbitmq